import 'package:flutter/material.dart';

class CustomLoadingCategories extends StatelessWidget {
  const CustomLoadingCategories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}